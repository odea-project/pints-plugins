name: intra_run_components
version: 0.1.0
license: MIT
compat:
  pints-core-sql: ">=1.0,<2.0"
files:
  schema: plugin.sql
  materialize: materialize.sql
  export: export.sql
description: >
  Minimal intra-run component mapping that assigns a component ID to each feature
  within a run. Stores sample_id, run_id, feature_id, intra_run_component_id.
